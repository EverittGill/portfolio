// this is the header component. It will render the header when used
function Header () {
    return( <>
    <div>
        <p> The header component is connected and is rendering</p>
    </div>
    </>
    )
}

export default Header